import 'package:flutter/material.dart';

class BovinesOutputPage extends StatelessWidget{
  const BovinesOutputPage({super.key});
  @override
  Widget build(BuildContext context){
    return Text("BovinesOutputPage asds");
  }
}